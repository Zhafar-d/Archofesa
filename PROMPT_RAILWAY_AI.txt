═══════════════════════════════════════════════════════════════════════════
   🤖 PROMPT UNTUK RAILWAY AI AGENT
═══════════════════════════════════════════════════════════════════════════

Copy-paste prompt ini ke Railway AI agent:

═══════════════════════════════════════════════════════════════════════════

I'm deploying a Laravel 11 application on Railway and getting "500 | SERVER ERROR" on all pages. Here's the information about my project:

**Project Details:**
- Framework: Laravel 11
- PHP Version: 8.2+
- Database: MySQL
- Frontend: Vite with Tailwind CSS
- Repository: https://github.com/Zhafar-d/Archofesa.git

**Current Issue:**
All pages return 500 error, including the homepage. I've already pushed the latest code to GitHub and Railway has deployed it.

**What I've tried:**
1. Added diagnostic routes in routes/web.php but they return 404
2. Attempted to clear cache via Railway Shell
3. Checked environment variables

**Questions:**
1. Can you check my deployment logs and identify the specific error causing the 500?
2. Is my APP_KEY properly set in environment variables?
3. Are database migrations running successfully?
4. Is the build command properly building Vite assets?
5. What environment variables am I missing?

**My railway.json config:**
```json
{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "releaseCommand": "php artisan cache:clear && php artisan config:clear && php artisan route:clear && php artisan view:clear && php artisan migrate --force && php artisan storage:link"
  }
}
```

**Expected setup:**
- Build Command: npm install && npm run build && composer install --optimize-autoloader --no-dev
- Start Command: php artisan serve --host=0.0.0.0 --port=$PORT
- Environment: production

Please help me:
1. Identify the root cause of the 500 error from the logs
2. List all required environment variables that might be missing
3. Suggest specific commands I should run in Railway Shell
4. Check if my build/start commands are correct
5. Verify if my database is properly connected

═══════════════════════════════════════════════════════════════════════════
   ATAU GUNAKAN PROMPT YANG LEBIH SPESIFIK INI:
═══════════════════════════════════════════════════════════════════════════

My Laravel 11 app returns 500 error on Railway. Please help:

1. Check my deployment logs and tell me the exact error message
2. Verify if APP_KEY environment variable is set
3. Check if database connection is working (DB_HOST, DB_DATABASE, etc)
4. Confirm if composer dependencies are installed
5. Verify if Vite assets are built (npm run build)
6. Check if migrations ran successfully
7. List any missing environment variables
8. Suggest specific fix commands to run in Railway Shell

My repository: https://github.com/Zhafar-d/Archofesa.git
Latest commit: 297d453

═══════════════════════════════════════════════════════════════════════════
   ATAU PROMPT SINGKAT (UNTUK QUICK DIAGNOSIS):
═══════════════════════════════════════════════════════════════════════════

Laravel 11 app on Railway shows 500 error. Can you:
1. Check deployment logs for errors
2. Verify APP_KEY is set
3. Check database connection
4. List missing environment variables
5. Suggest fix commands

Repo: https://github.com/Zhafar-d/Archofesa.git

═══════════════════════════════════════════════════════════════════════════
   FOLLOW-UP QUESTIONS (Kalau AI butuh info lebih):
═══════════════════════════════════════════════════════════════════════════

Jika AI agent bertanya, berikan info ini:

**Q: What's in your .env file?**
A: I have a local .env but Railway should use its own environment variables. 
   Please check if these are set in Railway:
   - APP_KEY (base64:...)
   - APP_ENV=production
   - APP_URL=https://archofesa-production.up.railway.app
   - DB_* (from MySQL service)
   - SESSION_DRIVER=database
   - CACHE_STORE=database

**Q: What does your composer.json require?**
A: Laravel 11, PHP ^8.2, MySQL/MariaDB, standard Laravel packages

**Q: What's your database structure?**
A: Tables: users, rooms, bookings, payments, sessions, cache
   Migrations are in database/migrations/

**Q: What routes are defined?**
A: 
   - Public routes: /, /about, /rooms, /gallery, /contact
   - Auth routes: /login, /register
   - Admin routes: /admin/dashboard, /admin/kamar, /admin/bookings
   - Customer routes: /customer/dashboard
   - Diagnostic routes: /diagnostic/basic, /diagnostic/db

**Q: What's your vite.config.js?**
A: Standard Laravel Vite config with input: ['resources/css/app.css', 'resources/js/app.js']

═══════════════════════════════════════════════════════════════════════════
   INFORMASI TAMBAHAN UNTUK AI:
═══════════════════════════════════════════════════════════════════════════

**Recent changes that might be relevant:**
1. Added multiple diagnostic routes for troubleshooting
2. Removed debug routes that caused 404 errors
3. Fixed duplicate closing tags in blade templates
4. Added deploy.sh script for deployment
5. Added custom 500 error page
6. Added logging in KamarController

**Expected behavior:**
- Homepage should display landing page
- /admin/kamar should show room management (after login)
- /admin/kamar/{id}/edit was returning 500 before, now whole site is 500

**My Railway setup:**
- Service: Web service connected to GitHub
- Database: MySQL (should be provisioned)
- Auto-deploy: Enabled on main branch

═══════════════════════════════════════════════════════════════════════════
   HASIL YANG DIHARAPKAN DARI AI:
═══════════════════════════════════════════════════════════════════════════

Saya harapkan AI agent bisa memberikan:

1. ✅ Error message spesifik dari logs
2. ✅ Daftar environment variables yang missing
3. ✅ Command spesifik untuk fix (bisa langsung execute)
4. ✅ Konfirmasi apakah build/start command sudah benar
5. ✅ Saran konfigurasi Railway yang optimal untuk Laravel 11

═══════════════════════════════════════════════════════════════════════════
   TIPS BERBICARA DENGAN RAILWAY AI:
═══════════════════════════════════════════════════════════════════════════

✅ DO:
- Berikan URL repository GitHub
- Sebutkan framework dan versi (Laravel 11)
- Tanyakan hal spesifik (check logs, verify env vars)
- Minta AI untuk execute commands jika bisa
- Tanyakan best practices Railway untuk Laravel

❌ DON'T:
- Jangan paste seluruh code
- Jangan tanya hal yang terlalu general
- Jangan minta AI untuk fix tanpa diagnosis dulu

═══════════════════════════════════════════════════════════════════════════
   ALTERNATIF: TANYAKAN KE RAILWAY AI STEP BY STEP
═══════════════════════════════════════════════════════════════════════════

**Step 1:** "Can you check my deployment logs and show me any errors?"

**Step 2:** "Can you list all environment variables currently set in my project?"

**Step 3:** "Can you verify if APP_KEY is properly set?"

**Step 4:** "Can you run 'php artisan migrate:status' in shell?"

**Step 5:** "What build command should I use for Laravel 11 with Vite?"

**Step 6:** "Can you run these commands: php artisan route:clear && php artisan config:clear"

═══════════════════════════════════════════════════════════════════════════

Semoga Railway AI agent bisa membantu! 🚀
